## Дополнительно: если когда-то используется встроенный guest, разрешить не только localhost.
## Основная учётная запись задаётся в compose через RABBITMQ_DEFAULT_USER/PASS.
loopback_users.guest = false
